const fs=require('fs'); const zlib=require('zlib'); const path='c:/Users/Colin/Documents/Github/balatro-save-loader/test-files/save-modded.jkr'; try{ const buf=fs.readFileSync(path); const s=zlib.inflateRawSync(buf).toString('utf8'); const lastReturn = s.lastIndexOf('return '); const relevant = lastReturn !== -1 ? s.slice(lastReturn+7) : s; console.log('---PARSED START---'); console.log(relevant.slice(0,1000)); console.log('---TRY JSON.parse---'); const stringKeys = /\[\"(.*?)\"\]=/g; const numberKeys = /\[(\d+)\]=/g; const trailingCommas = /,}/g; const jsonStr = relevant.replace(stringKeys, '"$1":').replace(numberKeys, '"NOSTRING_$1":').replace(trailingCommas, '}'); const obj = JSON.parse(jsonStr); console.log('OK parsed root keys:', Object.keys(obj).slice(0,20)); }catch(e){ console.error('ERR', e.message); }